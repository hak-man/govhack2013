#!/usr/bin/gawk -f

BEGIN {
    FS=",";
    OFS=",";
}

/^[^,]* \([^,]*\),/ {
    match ($1, /^(.*) \((.*)\)$/, m);
    print m[1] "," m[2]"," $2 , $3, $4, $5, $6
}


/^[^(]*,/ {
    print $1 ",," $2, $3, $4, $5, $6
}
